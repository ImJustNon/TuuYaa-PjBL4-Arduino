void SetupModulePins(){
  pinMode(alertBuzzerPin, OUTPUT); // ตั้งค่าพิน Buzzer เป็น OUTPUT
  pinMode(stopSwitchPin, INPUT_PULLUP); // ตั้งค่าพิน stopSwitchPin เป็น INPUT_PULLUP
  pinMode(relay_1, OUTPUT); // ตั้งค่าพิน relay_1 เป็น OUTPUT
  pinMode(relay_2, OUTPUT); // ตั้งค่าพิน relay_2 เป็น OUTPUT
  pinMode(relay_3, OUTPUT); // ตั้งค่าพิน relay_3 เป็น OUTPUT
  pinMode(relay_4, OUTPUT); // ตั้งค่าพิน relay_4 เป็น OUTPUT
  pinMode(relay_5, OUTPUT); // ตั้งค่าพิน relay_5 เป็น OUTPUT
  pinMode(relay_6, OUTPUT); // ตั้งค่าพิน relay_6 เป็น OUTPUT
  pinMode(relay_7, OUTPUT); // ตั้งค่าพิน relay_7 เป็น OUTPUT

  pinMode(relay_A, OUTPUT); // ตั้งค่าพิน relay_A เป็น OUTPUT
  pinMode(relay_B, OUTPUT); // ตั้งค่าพิน relay_B เป็น OUTPUT
  pinMode(relay_C, OUTPUT); // ตั้งค่าพิน relay_C เป็น OUTPUT
  pinMode(relay_D, OUTPUT); // ตั้งค่าพิน relay_D เป็น OUTPUT
}