void SetPinMode_B(){
  for(int i = 0; i < 2; i++){ // B1
    pinMode(LED_B1[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // B2
    pinMode(LED_B2[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // B3
    pinMode(LED_B3[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // B4
    pinMode(LED_B4[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // B5
    pinMode(LED_B5[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // B6
    pinMode(LED_B6[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // B7
    pinMode(LED_B7[i], OUTPUT);
  }
}