void SetPinMode_D(){
  for(int i = 0; i < 2; i++){ // D1
    pinMode(LED_D1[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // D2
    pinMode(LED_D2[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // D3
    pinMode(LED_D3[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // D4
    pinMode(LED_D4[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // D5
    pinMode(LED_D5[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // D6
    pinMode(LED_D6[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // D7
    pinMode(LED_D7[i], OUTPUT);
  }
}