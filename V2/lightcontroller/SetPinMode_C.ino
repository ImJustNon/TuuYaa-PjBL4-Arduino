void SetPinMode_C(){
  for(int i = 0; i < 2; i++){ // C1
    pinMode(LED_C1[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // C2
    pinMode(LED_C2[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // C3
    pinMode(LED_C3[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // C4
    pinMode(LED_C4[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // C5
    pinMode(LED_C5[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // C6
    pinMode(LED_C6[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // C7
    pinMode(LED_C7[i], OUTPUT);
  }
}