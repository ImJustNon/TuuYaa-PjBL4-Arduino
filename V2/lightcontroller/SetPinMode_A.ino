void SetPinMode_A(){
  for(int i = 0; i < 2; i++){ // A1
    pinMode(LED_A1[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // A2
    pinMode(LED_A2[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // A3
    pinMode(LED_A3[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // A4
    pinMode(LED_A4[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // A5
    pinMode(LED_A5[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // A6
    pinMode(LED_A6[i], OUTPUT);
  }
  for(int i = 0; i < 2; i++){ // A7
    pinMode(LED_A7[i], OUTPUT);
  }
}